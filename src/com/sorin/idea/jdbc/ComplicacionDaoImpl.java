/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.sorin.idea.jdbc;

import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

import com.sorin.idea.dao.*;
import com.sorin.idea.dto.*;
import com.sorin.idea.exceptions.*;
import com.sorin.idea.factory.*;

public class ComplicacionDaoImpl extends AbstractDAO implements ComplicacionDao
{
	private AuditoriaDao auditoriaDao = new AuditoriaDao();
    private AuditoriaDto auditoriaDto = null;
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT IDCOMPL, TIPO, CLASE, CARACTER FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( IDCOMPL, TIPO, CLASE, CARACTER ) VALUES ( ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET IDCOMPL = ?, TIPO = ?, CLASE = ?, CARACTER = ? WHERE IDCOMPL = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE IDCOMPL = ?";

	/** 
	 * Index of column IDCOMPL
	 */
	protected static final int COLUMN_IDCOMPL = 1;

	/** 
	 * Index of column TIPO
	 */
	protected static final int COLUMN_TIPO = 2;

	/** 
	 * Index of column CLASE
	 */
	protected static final int COLUMN_CLASE = 3;

	/** 
	 * Index of column CARACTER
	 */
	protected static final int COLUMN_CARACTER = 4;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 4;

	/** 
	 * Index of primary-key column IDCOMPL
	 */
	protected static final int PK_COLUMN_IDCOMPL = 1;

	/** 
	 * Inserts a new row in the complicacion table.
	 */
	public ComplicacionPk insert(Complicacion dto) throws ComplicacionDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT, Statement.RETURN_GENERATED_KEYS );
			int index = 1;
			stmt.setInt( index++, dto.getIdcompl() );
			stmt.setString( index++, dto.getTipo() );
			stmt.setString( index++, dto.getClase() );
			if (dto.isCaracterNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setInt( index++, dto.getCaracter() );
			}
		
			//System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			//System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			
			// retrieve values from auto-increment columns
			rs = stmt.getGeneratedKeys();
			if (rs != null && rs.next()) {
				dto.setIdcompl( rs.getInt( 1 ) );
			}
			auditoriaDto.setAccion(AuditoriaDto.insercion);
			auditoriaDto.setDescri(dto.toString());
			auditoriaDao.escribeAuditoria(auditoriaDto);
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ComplicacionDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the complicacion table.
	 */
	public void update(ComplicacionPk pk, Complicacion dto) throws ComplicacionDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			//System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setInt( index++, dto.getIdcompl() );
			stmt.setString( index++, dto.getTipo() );
			stmt.setString( index++, dto.getClase() );
			if (dto.isCaracterNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setInt( index++, dto.getCaracter() );
			}
		
			stmt.setInt( 5, pk.getIdcompl() );
			int rows = stmt.executeUpdate();
			auditoriaDto.setAccion(AuditoriaDto.modificacion);
			auditoriaDto.setDescri(dto.toString());
			auditoriaDao.escribeAuditoria(auditoriaDto);
			reset(dto);
			long t2 = System.currentTimeMillis();
			//System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ComplicacionDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the complicacion table.
	 */
	public void delete(ComplicacionPk pk) throws ComplicacionDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			//System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getIdcompl() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			//System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			auditoriaDto.setAccion(AuditoriaDto.borrado);
			auditoriaDto.setDescri(pk.toString());
			auditoriaDao.escribeAuditoria(auditoriaDto);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ComplicacionDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the complicacion table that matches the specified primary-key value.
	 */
	public Complicacion findByPrimaryKey(ComplicacionPk pk) throws ComplicacionDaoException
	{
		return findByPrimaryKey( pk.getIdcompl() );
	}

	/** 
	 * Returns all rows from the complicacion table that match the criteria 'IDCOMPL = :idcompl'.
	 */
	public Complicacion findByPrimaryKey(int idcompl) throws ComplicacionDaoException
	{
		Complicacion ret[] = findByDynamicSelect( SQL_SELECT + " WHERE IDCOMPL = ?", new Object[] {  new Integer(idcompl) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the complicacion table that match the criteria ''.
	 */
	public Complicacion[] findAll() throws ComplicacionDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY IDCOMPL", null );
	}

	/** 
	 * Returns all rows from the complicacion table that match the criteria 'IDCOMPL = :idcompl'.
	 */
	public Complicacion[] findWhereIdcomplEquals(int idcompl) throws ComplicacionDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE IDCOMPL = ? ORDER BY IDCOMPL", new Object[] {  new Integer(idcompl) } );
	}

	/** 
	 * Returns all rows from the complicacion table that match the criteria 'TIPO = :tipo'.
	 */
	public Complicacion[] findWhereTipoEquals(String tipo) throws ComplicacionDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE TIPO = ? ORDER BY TIPO", new Object[] { tipo } );
	}

	/** 
	 * Returns all rows from the complicacion table that match the criteria 'CLASE = :clase'.
	 */
	public Complicacion[] findWhereClaseEquals(String clase) throws ComplicacionDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE CLASE = ? ORDER BY CLASE", new Object[] { clase } );
	}

	/** 
	 * Returns all rows from the complicacion table that match the criteria 'CARACTER = :caracter'.
	 */
	public Complicacion[] findWhereCaracterEquals(int caracter) throws ComplicacionDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE CARACTER = ? ORDER BY CARACTER", new Object[] {  new Integer(caracter) } );
	}

	/**
	 * Method 'ComplicacionDaoImpl'
	 * 
	 */
	public ComplicacionDaoImpl()
	{
	}
	/**
	 * Method 'ComplicacionDaoImpl'
	 * 
	 */
	public ComplicacionDaoImpl(String usuario)
	{
		this.auditoriaDto = new AuditoriaDto(usuario, getTableName());
	}
	public ComplicacionDaoImpl(final java.sql.Connection userConn,String usuario)
	{
		this.userConn = userConn;
		this.auditoriaDto = new AuditoriaDto(usuario, getTableName());
	}

	/**
	 * Method 'ComplicacionDaoImpl'
	 * 
	 * @param userConn
	 */
	public ComplicacionDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "idea.complicacion";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected Complicacion fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			Complicacion dto = new Complicacion();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected Complicacion[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			Complicacion dto = new Complicacion();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		Complicacion ret[] = new Complicacion[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(Complicacion dto, ResultSet rs) throws SQLException
	{
		dto.setIdcompl( rs.getInt( COLUMN_IDCOMPL ) );
		dto.setTipo( rs.getString( COLUMN_TIPO ) );
		dto.setClase( rs.getString( COLUMN_CLASE ) );
		dto.setCaracter( rs.getInt( COLUMN_CARACTER ) );
		if (rs.wasNull()) {
			dto.setCaracterNull( true );
		}
		
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(Complicacion dto)
	{
	}

	/** 
	 * Returns all rows from the complicacion table that match the specified arbitrary SQL statement
	 */
	public Complicacion[] findByDynamicSelect(String sql, Object[] sqlParams) throws ComplicacionDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			//System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ComplicacionDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the complicacion table that match the specified arbitrary SQL statement
	 */
	public Complicacion[] findByDynamicWhere(String sql, Object[] sqlParams) throws ComplicacionDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			//System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ComplicacionDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
