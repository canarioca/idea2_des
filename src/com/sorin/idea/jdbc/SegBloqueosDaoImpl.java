/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.sorin.idea.jdbc;

import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

import com.sorin.idea.dao.*;
import com.sorin.idea.dto.*;
import com.sorin.idea.exceptions.*;
import com.sorin.idea.factory.*;

public class SegBloqueosDaoImpl extends AbstractDAO implements SegBloqueosDao
{
	private AuditoriaDao auditoriaDao = new AuditoriaDao();
    private AuditoriaDto auditoriaDto = null;
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT IDBLOQUEO, BAV1, BAV2, BAV3 FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( IDBLOQUEO, BAV1, BAV2, BAV3 ) VALUES ( ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET IDBLOQUEO = ?, BAV1 = ?, BAV2 = ?, BAV3 = ? WHERE IDBLOQUEO = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE IDBLOQUEO = ?";

	/** 
	 * Index of column IDBLOQUEO
	 */
	protected static final int COLUMN_IDBLOQUEO = 1;

	/** 
	 * Index of column BAV1
	 */
	protected static final int COLUMN_BAV1 = 2;

	/** 
	 * Index of column BAV2
	 */
	protected static final int COLUMN_BAV2 = 3;

	/** 
	 * Index of column BAV3
	 */
	protected static final int COLUMN_BAV3 = 4;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 4;

	/** 
	 * Index of primary-key column IDBLOQUEO
	 */
	protected static final int PK_COLUMN_IDBLOQUEO = 1;

	/** 
	 * Inserts a new row in the seg_bloqueos table.
	 */
	public SegBloqueosPk insert(SegBloqueos dto) throws SegBloqueosDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT, Statement.RETURN_GENERATED_KEYS );
			int index = 1;
			if (dto.getIdbloqueo() != null) {
				stmt.setInt( index++, dto.getIdbloqueo().intValue() );
			} else {
				stmt.setNull(index++, java.sql.Types.INTEGER);
			}
		
			stmt.setString( index++, dto.getBav1() );
			stmt.setString( index++, dto.getBav2() );
			stmt.setString( index++, dto.getBav3() );
			//System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			//System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		
			// retrieve values from auto-increment columns
			rs = stmt.getGeneratedKeys();
			if (rs != null && rs.next()) {
				dto.setIdbloqueo( new Integer( rs.getInt(1) ) );
			}
			auditoriaDto.setAccion(AuditoriaDto.insercion);
			auditoriaDto.setDescri(dto.toString());
			auditoriaDao.escribeAuditoria(auditoriaDto);
		
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new SegBloqueosDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the seg_bloqueos table.
	 */
	public void update(SegBloqueosPk pk, SegBloqueos dto) throws SegBloqueosDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			//System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			if (dto.getIdbloqueo() != null) {
				stmt.setInt( index++, dto.getIdbloqueo().intValue() );
			} else {
				stmt.setNull(index++, java.sql.Types.INTEGER);
			}
		
			stmt.setString( index++, dto.getBav1() );
			stmt.setString( index++, dto.getBav2() );
			stmt.setString( index++, dto.getBav3() );
			if (pk.getIdbloqueo() != null) {
				stmt.setInt( 5, pk.getIdbloqueo().intValue() );
			} else {
				stmt.setNull(5, java.sql.Types.INTEGER);
			}
		
			int rows = stmt.executeUpdate();
			auditoriaDto.setAccion(AuditoriaDto.modificacion);
			auditoriaDto.setDescri(dto.toString());
			auditoriaDao.escribeAuditoria(auditoriaDto);
			reset(dto);
			long t2 = System.currentTimeMillis();
			//System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new SegBloqueosDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the seg_bloqueos table.
	 */
	public void delete(SegBloqueosPk pk) throws SegBloqueosDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			//System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			if (pk.getIdbloqueo() != null) {
				stmt.setInt( 1, pk.getIdbloqueo().intValue() );
			} else {
				stmt.setNull(1, java.sql.Types.INTEGER);
			}
		
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			//System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			auditoriaDto.setAccion(AuditoriaDto.borrado);
			auditoriaDto.setDescri(pk.toString());
			auditoriaDao.escribeAuditoria(auditoriaDto);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new SegBloqueosDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the seg_bloqueos table that matches the specified primary-key value.
	 */
	public SegBloqueos findByPrimaryKey(SegBloqueosPk pk) throws SegBloqueosDaoException
	{
		return findByPrimaryKey( pk.getIdbloqueo() );
	}

	/** 
	 * Returns all rows from the seg_bloqueos table that match the criteria 'IDBLOQUEO = :idbloqueo'.
	 */
	public SegBloqueos findByPrimaryKey(Integer idbloqueo) throws SegBloqueosDaoException
	{
		SegBloqueos ret[] = findByDynamicSelect( SQL_SELECT + " WHERE IDBLOQUEO = ?", new Object[] { idbloqueo } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the seg_bloqueos table that match the criteria ''.
	 */
	public SegBloqueos[] findAll() throws SegBloqueosDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY IDBLOQUEO", null );
	}

	/** 
	 * Returns all rows from the seg_bloqueos table that match the criteria 'IDBLOQUEO = :idbloqueo'.
	 */
	public SegBloqueos[] findWhereIdbloqueoEquals(int idbloqueo) throws SegBloqueosDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE IDBLOQUEO = ? ORDER BY IDBLOQUEO", new Object[] {  new Integer(idbloqueo) } );
	}

	/** 
	 * Returns all rows from the seg_bloqueos table that match the criteria 'BAV1 = :bav1'.
	 */
	public SegBloqueos[] findWhereBav1Equals(String bav1) throws SegBloqueosDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE BAV1 = ? ORDER BY BAV1", new Object[] { bav1 } );
	}

	/** 
	 * Returns all rows from the seg_bloqueos table that match the criteria 'BAV2 = :bav2'.
	 */
	public SegBloqueos[] findWhereBav2Equals(String bav2) throws SegBloqueosDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE BAV2 = ? ORDER BY BAV2", new Object[] { bav2 } );
	}

	/** 
	 * Returns all rows from the seg_bloqueos table that match the criteria 'BAV3 = :bav3'.
	 */
	public SegBloqueos[] findWhereBav3Equals(String bav3) throws SegBloqueosDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE BAV3 = ? ORDER BY BAV3", new Object[] { bav3 } );
	}

	/**
	 * Method 'SegBloqueosDaoImpl'
	 * 
	 */
	public SegBloqueosDaoImpl()
	{
	}
	/**
	 * Method 'SegBloqueosDaoImpl'
	 * 
	 */
	public SegBloqueosDaoImpl(String usuario)
	{
		this.auditoriaDto = new AuditoriaDto(usuario, getTableName());
	}
	public SegBloqueosDaoImpl(final java.sql.Connection userConn,String usuario)
	{
		this.userConn = userConn;
		this.auditoriaDto = new AuditoriaDto(usuario, getTableName());
	}

	/**
	 * Method 'SegBloqueosDaoImpl'
	 * 
	 * @param userConn
	 */
	public SegBloqueosDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "idea.seg_bloqueos";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected SegBloqueos fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			SegBloqueos dto = new SegBloqueos();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected SegBloqueos[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			SegBloqueos dto = new SegBloqueos();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		SegBloqueos ret[] = new SegBloqueos[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(SegBloqueos dto, ResultSet rs) throws SQLException
	{
		dto.setIdbloqueo( new Integer( rs.getInt(COLUMN_IDBLOQUEO) ) );
		dto.setBav1( rs.getString( COLUMN_BAV1 ) );
		dto.setBav2( rs.getString( COLUMN_BAV2 ) );
		dto.setBav3( rs.getString( COLUMN_BAV3 ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(SegBloqueos dto)
	{
	}

	/** 
	 * Returns all rows from the seg_bloqueos table that match the specified arbitrary SQL statement
	 */
	public SegBloqueos[] findByDynamicSelect(String sql, Object[] sqlParams) throws SegBloqueosDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			//System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new SegBloqueosDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the seg_bloqueos table that match the specified arbitrary SQL statement
	 */
	public SegBloqueos[] findByDynamicWhere(String sql, Object[] sqlParams) throws SegBloqueosDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			//System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new SegBloqueosDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
