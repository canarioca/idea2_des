/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.sorin.idea.jdbc;

import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

import com.sorin.idea.dao.*;
import com.sorin.idea.dto.*;
import com.sorin.idea.exceptions.*;
import com.sorin.idea.factory.*;

public class ImpAtpDaoImpl extends AbstractDAO implements ImpAtpDao
{
	private AuditoriaDao auditoriaDao = new AuditoriaDao();
    private AuditoriaDto auditoriaDto = null;
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT IDATP, IDZONA, TIPO, NUMSEC, CICLOSEC, ACOPLAMIENTO, DECREMENTO FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( IDATP, IDZONA, TIPO, NUMSEC, CICLOSEC, ACOPLAMIENTO, DECREMENTO ) VALUES ( ?, ?, ?, ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET IDATP = ?, IDZONA = ?, TIPO = ?, NUMSEC = ?, CICLOSEC = ?, ACOPLAMIENTO = ?, DECREMENTO = ? WHERE IDATP = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE IDATP = ?";

	/** 
	 * Index of column IDATP
	 */
	protected static final int COLUMN_IDATP = 1;

	/** 
	 * Index of column IDZONA
	 */
	protected static final int COLUMN_IDZONA = 2;

	/** 
	 * Index of column TIPO
	 */
	protected static final int COLUMN_TIPO = 3;

	/** 
	 * Index of column NUMSEC
	 */
	protected static final int COLUMN_NUMSEC = 4;

	/** 
	 * Index of column CICLOSEC
	 */
	protected static final int COLUMN_CICLOSEC = 5;

	/** 
	 * Index of column ACOPLAMIENTO
	 */
	protected static final int COLUMN_ACOPLAMIENTO = 6;

	/** 
	 * Index of column DECREMENTO
	 */
	protected static final int COLUMN_DECREMENTO = 7;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 7;

	/** 
	 * Index of primary-key column IDATP
	 */
	protected static final int PK_COLUMN_IDATP = 1;

	/** 
	 * Inserts a new row in the imp_atp table.
	 */
	public ImpAtpPk insert(ImpAtp dto) throws ImpAtpDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT, Statement.RETURN_GENERATED_KEYS );
			int index = 1;
			if (dto.getIdatp() != null) {
				stmt.setInt( index++, dto.getIdatp().intValue() );
			} else {
				stmt.setNull(index++, java.sql.Types.INTEGER);
			}
		
			if (dto.getIdzona() != null) {
				stmt.setInt( index++, dto.getIdzona().intValue() );
			} else {
				stmt.setNull(index++, java.sql.Types.INTEGER);
			}
		
			stmt.setString( index++, dto.getTipo() );
			stmt.setString( index++, dto.getNumsec() );
			stmt.setString( index++, dto.getCiclosec() );
			stmt.setString( index++, dto.getAcoplamiento() );
			stmt.setString( index++, dto.getDecremento() );
			//System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			//System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		
			// retrieve values from auto-increment columns
			rs = stmt.getGeneratedKeys();
			if (rs != null && rs.next()) {
				dto.setIdatp( new Integer( rs.getInt(1) ) );
			}
			auditoriaDto.setAccion(AuditoriaDto.insercion);
			auditoriaDto.setDescri(dto.toString());
			auditoriaDao.escribeAuditoria(auditoriaDto);
		
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ImpAtpDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the imp_atp table.
	 */
	public void update(ImpAtpPk pk, ImpAtp dto) throws ImpAtpDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			//System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			if (dto.getIdatp() != null) {
				stmt.setInt( index++, dto.getIdatp().intValue() );
			} else {
				stmt.setNull(index++, java.sql.Types.INTEGER);
			}
		
			if (dto.getIdzona() != null) {
				stmt.setInt( index++, dto.getIdzona().intValue() );
			} else {
				stmt.setNull(index++, java.sql.Types.INTEGER);
			}
		
			stmt.setString( index++, dto.getTipo() );
			stmt.setString( index++, dto.getNumsec() );
			stmt.setString( index++, dto.getCiclosec() );
			stmt.setString( index++, dto.getAcoplamiento() );
			stmt.setString( index++, dto.getDecremento() );
			if (pk.getIdatp() != null) {
				stmt.setInt( 8, pk.getIdatp().intValue() );
			} else {
				stmt.setNull(8, java.sql.Types.INTEGER);
			}
		
			int rows = stmt.executeUpdate();
			auditoriaDto.setAccion(AuditoriaDto.modificacion);
			auditoriaDto.setDescri(dto.toString());
			auditoriaDao.escribeAuditoria(auditoriaDto);
			reset(dto);
			long t2 = System.currentTimeMillis();
			//System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ImpAtpDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the imp_atp table.
	 */
	public void delete(ImpAtpPk pk) throws ImpAtpDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			//System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			if (pk.getIdatp() != null) {
				stmt.setInt( 1, pk.getIdatp().intValue() );
			} else {
				stmt.setNull(1, java.sql.Types.INTEGER);
			}
		
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			//System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
			auditoriaDto.setAccion(AuditoriaDto.borrado);
			auditoriaDto.setDescri(pk.toString());
			auditoriaDao.escribeAuditoria(auditoriaDto);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ImpAtpDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the imp_atp table that matches the specified primary-key value.
	 */
	public ImpAtp findByPrimaryKey(ImpAtpPk pk) throws ImpAtpDaoException
	{
		return findByPrimaryKey( pk.getIdatp() );
	}

	/** 
	 * Returns all rows from the imp_atp table that match the criteria 'IDATP = :idatp'.
	 */
	public ImpAtp findByPrimaryKey(Integer idatp) throws ImpAtpDaoException
	{
		ImpAtp ret[] = findByDynamicSelect( SQL_SELECT + " WHERE IDATP = ?", new Object[] { idatp } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the imp_atp table that match the criteria ''.
	 */
	public ImpAtp[] findAll() throws ImpAtpDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY IDATP", null );
	}

	/** 
	 * Returns all rows from the imp_atp table that match the criteria 'IDZONA = :idzona'.
	 */
	public ImpAtp[] findByImpZona(int idzona) throws ImpAtpDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE IDZONA = ?", new Object[] {  new Integer(idzona) } );
	}

	/** 
	 * Returns all rows from the imp_atp table that match the criteria 'IDATP = :idatp'.
	 */
	public ImpAtp[] findWhereIdatpEquals(int idatp) throws ImpAtpDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE IDATP = ? ORDER BY IDATP", new Object[] {  new Integer(idatp) } );
	}

	/** 
	 * Returns all rows from the imp_atp table that match the criteria 'IDZONA = :idzona'.
	 */
	public ImpAtp[] findWhereIdzonaEquals(int idzona) throws ImpAtpDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE IDZONA = ? ORDER BY IDZONA", new Object[] {  new Integer(idzona) } );
	}

	/** 
	 * Returns all rows from the imp_atp table that match the criteria 'TIPO = :tipo'.
	 */
	public ImpAtp[] findWhereTipoEquals(String tipo) throws ImpAtpDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE TIPO = ? ORDER BY TIPO", new Object[] { tipo } );
	}

	/** 
	 * Returns all rows from the imp_atp table that match the criteria 'NUMSEC = :numsec'.
	 */
	public ImpAtp[] findWhereNumsecEquals(String numsec) throws ImpAtpDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE NUMSEC = ? ORDER BY NUMSEC", new Object[] { numsec } );
	}

	/** 
	 * Returns all rows from the imp_atp table that match the criteria 'CICLOSEC = :ciclosec'.
	 */
	public ImpAtp[] findWhereCiclosecEquals(String ciclosec) throws ImpAtpDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE CICLOSEC = ? ORDER BY CICLOSEC", new Object[] { ciclosec } );
	}

	/** 
	 * Returns all rows from the imp_atp table that match the criteria 'ACOPLAMIENTO = :acoplamiento'.
	 */
	public ImpAtp[] findWhereAcoplamientoEquals(String acoplamiento) throws ImpAtpDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ACOPLAMIENTO = ? ORDER BY ACOPLAMIENTO", new Object[] { acoplamiento } );
	}

	/** 
	 * Returns all rows from the imp_atp table that match the criteria 'DECREMENTO = :decremento'.
	 */
	public ImpAtp[] findWhereDecrementoEquals(String decremento) throws ImpAtpDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE DECREMENTO = ? ORDER BY DECREMENTO", new Object[] { decremento } );
	}

	/**
	 * Method 'ImpAtpDaoImpl'
	 * 
	 */
	public ImpAtpDaoImpl()
	{
	}
	/**
	 * Method 'ImpAtpDaoImpl'
	 * 
	 */
	public ImpAtpDaoImpl(String usuario)
	{
		this.auditoriaDto = new AuditoriaDto(usuario, getTableName());
	}
	public ImpAtpDaoImpl(final java.sql.Connection userConn,String usuario)
	{
		this.userConn = userConn;
		this.auditoriaDto = new AuditoriaDto(usuario, getTableName());
	}

	/**
	 * Method 'ImpAtpDaoImpl'
	 * 
	 * @param userConn
	 */
	public ImpAtpDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "idea.imp_atp";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected ImpAtp fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			ImpAtp dto = new ImpAtp();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected ImpAtp[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			ImpAtp dto = new ImpAtp();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		ImpAtp ret[] = new ImpAtp[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(ImpAtp dto, ResultSet rs) throws SQLException
	{
		dto.setIdatp( new Integer( rs.getInt(COLUMN_IDATP) ) );
		dto.setIdzona( new Integer( rs.getInt(COLUMN_IDZONA) ) );
		dto.setTipo( rs.getString( COLUMN_TIPO ) );
		dto.setNumsec( rs.getString( COLUMN_NUMSEC ) );
		dto.setCiclosec( rs.getString( COLUMN_CICLOSEC ) );
		dto.setAcoplamiento( rs.getString( COLUMN_ACOPLAMIENTO ) );
		dto.setDecremento( rs.getString( COLUMN_DECREMENTO ) );
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(ImpAtp dto)
	{
	}

	/** 
	 * Returns all rows from the imp_atp table that match the specified arbitrary SQL statement
	 */
	public ImpAtp[] findByDynamicSelect(String sql, Object[] sqlParams) throws ImpAtpDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			//System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ImpAtpDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the imp_atp table that match the specified arbitrary SQL statement
	 */
	public ImpAtp[] findByDynamicWhere(String sql, Object[] sqlParams) throws ImpAtpDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			//System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new ImpAtpDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
