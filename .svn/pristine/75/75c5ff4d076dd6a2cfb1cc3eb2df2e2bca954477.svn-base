/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.sorin.idea.jdbc;

import com.sorin.idea.dao.*;
import com.sorin.idea.factory.*;
import com.sorin.idea.dto.*;
import com.sorin.idea.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class RcPsicoNivelEstudiosDaoImpl extends AbstractDAO implements RcPsicoNivelEstudiosDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID_NIVEL_ESTUDIOS, ESTUDIO FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID_NIVEL_ESTUDIOS, ESTUDIO ) VALUES ( ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID_NIVEL_ESTUDIOS = ?, ESTUDIO = ? WHERE ID_NIVEL_ESTUDIOS = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID_NIVEL_ESTUDIOS = ?";

	/** 
	 * Index of column ID_NIVEL_ESTUDIOS
	 */
	protected static final int COLUMN_ID_NIVEL_ESTUDIOS = 1;

	/** 
	 * Index of column ESTUDIO
	 */
	protected static final int COLUMN_ESTUDIO = 2;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 2;

	/** 
	 * Index of primary-key column ID_NIVEL_ESTUDIOS
	 */
	protected static final int PK_COLUMN_ID_NIVEL_ESTUDIOS = 1;

	/** 
	 * Inserts a new row in the RC_PSICO_NIVEL_ESTUDIOS table.
	 */
	public RcPsicoNivelEstudiosPk insert(RcPsicoNivelEstudios dto) throws RcPsicoNivelEstudiosDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			StringBuffer values = new StringBuffer();
			sql.append( "INSERT INTO " + getTableName() + " (" );
			int modifiedCount = 0;
			if (dto.isIdNivelEstudiosModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_NIVEL_ESTUDIOS" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isEstudioModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ESTUDIO" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (modifiedCount==0) {
				// nothing to insert
				throw new IllegalStateException( "Nothing to insert" );
			}
		
			sql.append( ") VALUES (" );
			sql.append( values );
			sql.append( ")" );
			stmt = conn.prepareStatement( sql.toString() );
			int index = 1;
			if (dto.isIdNivelEstudiosModified()) {
				stmt.setLong( index++, dto.getIdNivelEstudios() );
			}
		
			if (dto.isEstudioModified()) {
				stmt.setString( index++, dto.getEstudio() );
			}
		
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			throw new RcPsicoNivelEstudiosDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the RC_PSICO_NIVEL_ESTUDIOS table.
	 */
	public void update(RcPsicoNivelEstudiosPk pk, RcPsicoNivelEstudios dto) throws RcPsicoNivelEstudiosDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			sql.append( "UPDATE " + getTableName() + " SET " );
			boolean modified = false;
			if (dto.isIdNivelEstudiosModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_NIVEL_ESTUDIOS=?" );
				modified=true;
			}
		
			if (dto.isEstudioModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ESTUDIO=?" );
				modified=true;
			}
		
			if (!modified) {
				// nothing to update
				return;
			}
		
			sql.append( " WHERE ID_NIVEL_ESTUDIOS=?" );
			stmt = conn.prepareStatement( sql.toString() );
			int index = 1;
			if (dto.isIdNivelEstudiosModified()) {
				stmt.setLong( index++, dto.getIdNivelEstudios() );
			}
		
			if (dto.isEstudioModified()) {
				stmt.setString( index++, dto.getEstudio() );
			}
		
			stmt.setLong( index++, pk.getIdNivelEstudios() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
		}
		catch (Exception _e) {
			throw new RcPsicoNivelEstudiosDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the RC_PSICO_NIVEL_ESTUDIOS table.
	 */
	public void delete(RcPsicoNivelEstudiosPk pk) throws RcPsicoNivelEstudiosDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setLong( 1, pk.getIdNivelEstudios() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
		}
		catch (Exception _e) {
			throw new RcPsicoNivelEstudiosDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the RC_PSICO_NIVEL_ESTUDIOS table that matches the specified primary-key value.
	 */
	public RcPsicoNivelEstudios findByPrimaryKey(RcPsicoNivelEstudiosPk pk) throws RcPsicoNivelEstudiosDaoException
	{
		return findByPrimaryKey( pk.getIdNivelEstudios() );
	}

	/** 
	 * Returns all rows from the RC_PSICO_NIVEL_ESTUDIOS table that match the criteria 'ID_NIVEL_ESTUDIOS = :idNivelEstudios'.
	 */
	public RcPsicoNivelEstudios findByPrimaryKey(long idNivelEstudios) throws RcPsicoNivelEstudiosDaoException
	{
		RcPsicoNivelEstudios ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID_NIVEL_ESTUDIOS = ?", new Object[] {  new Long(idNivelEstudios) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the RC_PSICO_NIVEL_ESTUDIOS table that match the criteria ''.
	 */
	public RcPsicoNivelEstudios[] findAll() throws RcPsicoNivelEstudiosDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID_NIVEL_ESTUDIOS", null );
	}

	/** 
	 * Returns all rows from the RC_PSICO_NIVEL_ESTUDIOS table that match the criteria 'ID_NIVEL_ESTUDIOS = :idNivelEstudios'.
	 */
	public RcPsicoNivelEstudios[] findWhereIdNivelEstudiosEquals(long idNivelEstudios) throws RcPsicoNivelEstudiosDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_NIVEL_ESTUDIOS = ? ORDER BY ID_NIVEL_ESTUDIOS", new Object[] {  new Long(idNivelEstudios) } );
	}

	/** 
	 * Returns all rows from the RC_PSICO_NIVEL_ESTUDIOS table that match the criteria 'ESTUDIO = :estudio'.
	 */
	public RcPsicoNivelEstudios[] findWhereEstudioEquals(String estudio) throws RcPsicoNivelEstudiosDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ESTUDIO = ? ORDER BY ESTUDIO", new Object[] { estudio } );
	}

	/**
	 * Method 'RcPsicoNivelEstudiosDaoImpl'
	 * 
	 */
	public RcPsicoNivelEstudiosDaoImpl()
	{
	}

	/**
	 * Method 'RcPsicoNivelEstudiosDaoImpl'
	 * 
	 * @param userConn
	 */
	public RcPsicoNivelEstudiosDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "IDEA.RC_PSICO_NIVEL_ESTUDIOS";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected RcPsicoNivelEstudios fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			RcPsicoNivelEstudios dto = new RcPsicoNivelEstudios();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected RcPsicoNivelEstudios[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			RcPsicoNivelEstudios dto = new RcPsicoNivelEstudios();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		RcPsicoNivelEstudios ret[] = new RcPsicoNivelEstudios[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(RcPsicoNivelEstudios dto, ResultSet rs) throws SQLException
	{
		dto.setIdNivelEstudios( rs.getLong( COLUMN_ID_NIVEL_ESTUDIOS ) );
		dto.setEstudio( rs.getString( COLUMN_ESTUDIO ) );
		reset(dto);
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(RcPsicoNivelEstudios dto)
	{
		dto.setIdNivelEstudiosModified( false );
		dto.setEstudioModified( false );
	}

	/** 
	 * Returns all rows from the RC_PSICO_NIVEL_ESTUDIOS table that match the specified arbitrary SQL statement
	 */
	public RcPsicoNivelEstudios[] findByDynamicSelect(String sql, Object[] sqlParams) throws RcPsicoNivelEstudiosDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			throw new RcPsicoNivelEstudiosDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the RC_PSICO_NIVEL_ESTUDIOS table that match the specified arbitrary SQL statement
	 */
	public RcPsicoNivelEstudios[] findByDynamicWhere(String sql, Object[] sqlParams) throws RcPsicoNivelEstudiosDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			throw new RcPsicoNivelEstudiosDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
