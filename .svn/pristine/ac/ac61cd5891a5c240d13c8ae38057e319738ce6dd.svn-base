/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.sorin.idea.jdbc;

import com.sorin.idea.dao.*;
import com.sorin.idea.factory.*;
import com.sorin.idea.dto.*;
import com.sorin.idea.exceptions.*;
import java.sql.Connection;
import java.util.Collection;
import java.sql.PreparedStatement;
import java.sql.Statement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.sql.Time;
import java.util.List;
import java.util.Iterator;
import java.util.ArrayList;

public class PlantillasDaoImpl extends AbstractDAO implements PlantillasDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID_PLANTILLA, TITULO_PLANTILLA, DESCRIPCION_PLANTILLA, TIPO_PROCEDIMIENTO FROM " + getTableName() + "";

	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID_PLANTILLA, TITULO_PLANTILLA, DESCRIPCION_PLANTILLA, TIPO_PROCEDIMIENTO ) VALUES ( ?, ?, ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID_PLANTILLA = ?, TITULO_PLANTILLA = ?, DESCRIPCION_PLANTILLA = ?, TIPO_PROCEDIMIENTO = ? WHERE ID_PLANTILLA = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID_PLANTILLA = ?";

	/** 
	 * Index of column ID_PLANTILLA
	 */
	protected static final int COLUMN_ID_PLANTILLA = 1;

	/** 
	 * Index of column TITULO_PLANTILLA
	 */
	protected static final int COLUMN_TITULO_PLANTILLA = 2;

	/** 
	 * Index of column DESCRIPCION_PLANTILLA
	 */
	protected static final int COLUMN_DESCRIPCION_PLANTILLA = 3;

	/** 
	 * Index of column TIPO_PROCEDIMIENTO
	 */
	protected static final int COLUMN_TIPO_PROCEDIMIENTO = 4;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 4;

	/** 
	 * Index of primary-key column ID_PLANTILLA
	 */
	protected static final int PK_COLUMN_ID_PLANTILLA = 1;

	/** 
	 * Inserts a new row in the plantillas table.
	 */
	public PlantillasPk insert(Plantillas dto) throws PlantillasDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_INSERT, Statement.RETURN_GENERATED_KEYS );
			int index = 1;
			stmt.setInt( index++, dto.getIdPlantilla() );
			stmt.setString( index++, dto.getTituloPlantilla() );
			stmt.setString( index++, dto.getDescripcionPlantilla() );
			if (dto.isTipoProcedimientoNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setInt( index++, dto.getTipoProcedimiento() );
			}
		
			System.out.println( "Executing " + SQL_INSERT + " with DTO: " + dto );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		
			// retrieve values from auto-increment columns
			rs = stmt.getGeneratedKeys();
			if (rs != null && rs.next()) {
				dto.setIdPlantilla( rs.getInt( 1 ) );
			}
		
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new PlantillasDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the plantillas table.
	 */
	public void update(PlantillasPk pk, Plantillas dto) throws PlantillasDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_UPDATE + " with DTO: " + dto );
			stmt = conn.prepareStatement( SQL_UPDATE );
			int index=1;
			stmt.setInt( index++, dto.getIdPlantilla() );
			stmt.setString( index++, dto.getTituloPlantilla() );
			stmt.setString( index++, dto.getDescripcionPlantilla() );
			if (dto.isTipoProcedimientoNull()) {
				stmt.setNull( index++, java.sql.Types.INTEGER );
			} else {
				stmt.setInt( index++, dto.getTipoProcedimiento() );
			}
		
			stmt.setInt( 5, pk.getIdPlantilla() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new PlantillasDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the plantillas table.
	 */
	public void delete(PlantillasPk pk) throws PlantillasDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			System.out.println( "Executing " + SQL_DELETE + " with PK: " + pk );
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setInt( 1, pk.getIdPlantilla() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			System.out.println( rows + " rows affected (" + (t2-t1) + " ms)" );
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new PlantillasDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the plantillas table that matches the specified primary-key value.
	 */
	public Plantillas findByPrimaryKey(PlantillasPk pk) throws PlantillasDaoException
	{
		return findByPrimaryKey( pk.getIdPlantilla() );
	}

	/** 
	 * Returns all rows from the plantillas table that match the criteria 'ID_PLANTILLA = :idPlantilla'.
	 */
	public Plantillas findByPrimaryKey(int idPlantilla) throws PlantillasDaoException
	{
		Plantillas ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID_PLANTILLA = ?", new Object[] {  new Integer(idPlantilla) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the plantillas table that match the criteria ''.
	 */
	public Plantillas[] findAll() throws PlantillasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID_PLANTILLA", null );
	}

	/** 
	 * Returns all rows from the plantillas table that match the criteria 'ID_PLANTILLA = :idPlantilla'.
	 */
	public Plantillas[] findWhereIdPlantillaEquals(int idPlantilla) throws PlantillasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_PLANTILLA = ? ORDER BY ID_PLANTILLA", new Object[] {  new Integer(idPlantilla) } );
	}

	/** 
	 * Returns all rows from the plantillas table that match the criteria 'TITULO_PLANTILLA = :tituloPlantilla'.
	 */
	public Plantillas[] findWhereTituloPlantillaEquals(String tituloPlantilla) throws PlantillasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE TITULO_PLANTILLA = ? ORDER BY TITULO_PLANTILLA", new Object[] { tituloPlantilla } );
	}

	/** 
	 * Returns all rows from the plantillas table that match the criteria 'DESCRIPCION_PLANTILLA = :descripcionPlantilla'.
	 */
	public Plantillas[] findWhereDescripcionPlantillaEquals(String descripcionPlantilla) throws PlantillasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE DESCRIPCION_PLANTILLA = ? ORDER BY DESCRIPCION_PLANTILLA", new Object[] { descripcionPlantilla } );
	}

	/** 
	 * Returns all rows from the plantillas table that match the criteria 'TIPO_PROCEDIMIENTO = :tipoProcedimiento'.
	 */
	public Plantillas[] findWhereTipoProcedimientoEquals(int tipoProcedimiento) throws PlantillasDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE TIPO_PROCEDIMIENTO = ? ORDER BY TIPO_PROCEDIMIENTO", new Object[] {  new Integer(tipoProcedimiento) } );
	}

	/**
	 * Method 'PlantillasDaoImpl'
	 * 
	 */
	public PlantillasDaoImpl()
	{
	}

	/**
	 * Method 'PlantillasDaoImpl'
	 * 
	 * @param userConn
	 */
	public PlantillasDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "idea.plantillas";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected Plantillas fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			Plantillas dto = new Plantillas();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected Plantillas[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			Plantillas dto = new Plantillas();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		Plantillas ret[] = new Plantillas[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(Plantillas dto, ResultSet rs) throws SQLException
	{
		dto.setIdPlantilla( rs.getInt( COLUMN_ID_PLANTILLA ) );
		dto.setTituloPlantilla( rs.getString( COLUMN_TITULO_PLANTILLA ) );
		dto.setDescripcionPlantilla( rs.getString( COLUMN_DESCRIPCION_PLANTILLA ) );
		dto.setTipoProcedimiento( rs.getInt( COLUMN_TIPO_PROCEDIMIENTO ) );
		if (rs.wasNull()) {
			dto.setTipoProcedimientoNull( true );
		}
		
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(Plantillas dto)
	{
	}

	/** 
	 * Returns all rows from the plantillas table that match the specified arbitrary SQL statement
	 */
	public Plantillas[] findByDynamicSelect(String sql, Object[] sqlParams) throws PlantillasDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new PlantillasDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the plantillas table that match the specified arbitrary SQL statement
	 */
	public Plantillas[] findByDynamicWhere(String sql, Object[] sqlParams) throws PlantillasDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			System.out.println( "Executing " + SQL );
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			_e.printStackTrace();
			throw new PlantillasDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

}
