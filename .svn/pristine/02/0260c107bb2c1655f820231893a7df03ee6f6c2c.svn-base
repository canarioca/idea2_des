/*
 * This source file was generated by FireStorm/DAO.
 * 
 * If you purchase a full license for FireStorm/DAO you can customize this header file.
 * 
 * For more information please visit http://www.codefutures.com/products/firestorm
 */

package com.sorin.idea.jdbc;

import java.sql.Connection;
import java.sql.PreparedStatement;
import java.sql.ResultSet;
import java.sql.SQLException;
import java.util.ArrayList;
import java.util.Collection;

import com.sorin.idea.dao.RcPsicoNivelSatisfaccionDao;
import com.sorin.idea.dto.RcPsicoNivelSatisfaccion;
import com.sorin.idea.dto.RcPsicoNivelSatisfaccionPk;
import com.sorin.idea.exceptions.RcPsicoNivelSatisfaccionDaoException;
import com.sorin.idea.exceptions.RcRehaProbSensorialesDaoException;

public class RcPsicoNivelSatisfaccionDaoImpl extends AbstractDAO implements RcPsicoNivelSatisfaccionDao
{
	/** 
	 * The factory class for this DAO has two versions of the create() method - one that
takes no arguments and one that takes a Connection argument. If the Connection version
is chosen then the connection will be stored in this attribute and will be used by all
calls to this DAO, otherwise a new Connection will be allocated for each operation.
	 */
	protected java.sql.Connection userConn;

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT = "SELECT ID_NIVEL_SATISFACCION, TIPO FROM " + getTableName() + "";

	/** 
	 * All finder methods in this class use this SELECT constant to build their queries
	 */
	protected final String SQL_SELECT_INNER_JOIN = "SELECT " + getTableName() + ".ID_NIVEL_SATISFACCION, " + getTableName() + ".TIPO FROM " + getTableName() + "";

	
	/** 
	 * Finder methods will pass this value to the JDBC setMaxRows method
	 */
	protected int maxRows;

	/** 
	 * SQL INSERT statement for this table
	 */
	protected final String SQL_INSERT = "INSERT INTO " + getTableName() + " ( ID_NIVEL_SATISFACCION, TIPO ) VALUES ( ?, ? )";

	/** 
	 * SQL UPDATE statement for this table
	 */
	protected final String SQL_UPDATE = "UPDATE " + getTableName() + " SET ID_NIVEL_SATISFACCION = ?, TIPO = ? WHERE ID_NIVEL_SATISFACCION = ?";

	/** 
	 * SQL DELETE statement for this table
	 */
	protected final String SQL_DELETE = "DELETE FROM " + getTableName() + " WHERE ID_NIVEL_SATISFACCION = ?";

	/** 
	 * Index of column ID_NIVEL_SATISFACCION
	 */
	protected static final int COLUMN_ID_NIVEL_SATISFACCION = 1;

	/** 
	 * Index of column TIPO
	 */
	protected static final int COLUMN_TIPO = 2;

	/** 
	 * Number of columns
	 */
	protected static final int NUMBER_OF_COLUMNS = 2;

	/** 
	 * Index of primary-key column ID_NIVEL_SATISFACCION
	 */
	protected static final int PK_COLUMN_ID_NIVEL_SATISFACCION = 1;

	/** 
	 * Inserts a new row in the RC_PSICO_NIVEL_SATISFACCION table.
	 */
	public RcPsicoNivelSatisfaccionPk insert(RcPsicoNivelSatisfaccion dto) throws RcPsicoNivelSatisfaccionDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			StringBuffer values = new StringBuffer();
			sql.append( "INSERT INTO " + getTableName() + " (" );
			int modifiedCount = 0;
			if (dto.isIdNivelSatisfaccionModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "ID_NIVEL_SATISFACCION" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (dto.isTipoModified()) {
				if (modifiedCount>0) {
					sql.append( ", " );
					values.append( ", " );
				}
		
				sql.append( "TIPO" );
				values.append( "?" );
				modifiedCount++;
			}
		
			if (modifiedCount==0) {
				// nothing to insert
				throw new IllegalStateException( "Nothing to insert" );
			}
		
			sql.append( ") VALUES (" );
			sql.append( values );
			sql.append( ")" );
			stmt = conn.prepareStatement( sql.toString() );
			int index = 1;
			if (dto.isIdNivelSatisfaccionModified()) {
				stmt.setLong( index++, dto.getIdNivelSatisfaccion() );
			}
		
			if (dto.isTipoModified()) {
				stmt.setString( index++, dto.getTipo() );
			}
		
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
			reset(dto);
			return dto.createPk();
		}
		catch (Exception _e) {
			throw new RcPsicoNivelSatisfaccionDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Updates a single row in the RC_PSICO_NIVEL_SATISFACCION table.
	 */
	public void update(RcPsicoNivelSatisfaccionPk pk, RcPsicoNivelSatisfaccion dto) throws RcPsicoNivelSatisfaccionDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			StringBuffer sql = new StringBuffer();
			sql.append( "UPDATE " + getTableName() + " SET " );
			boolean modified = false;
			if (dto.isIdNivelSatisfaccionModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "ID_NIVEL_SATISFACCION=?" );
				modified=true;
			}
		
			if (dto.isTipoModified()) {
				if (modified) {
					sql.append( ", " );
				}
		
				sql.append( "TIPO=?" );
				modified=true;
			}
		
			if (!modified) {
				// nothing to update
				return;
			}
		
			sql.append( " WHERE ID_NIVEL_SATISFACCION=?" );
			stmt = conn.prepareStatement( sql.toString() );
			int index = 1;
			if (dto.isIdNivelSatisfaccionModified()) {
				stmt.setLong( index++, dto.getIdNivelSatisfaccion() );
			}
		
			if (dto.isTipoModified()) {
				stmt.setString( index++, dto.getTipo() );
			}
		
			stmt.setLong( index++, pk.getIdNivelSatisfaccion() );
			int rows = stmt.executeUpdate();
			reset(dto);
			long t2 = System.currentTimeMillis();
		}
		catch (Exception _e) {
			throw new RcPsicoNivelSatisfaccionDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Deletes a single row in the RC_PSICO_NIVEL_SATISFACCION table.
	 */
	public void delete(RcPsicoNivelSatisfaccionPk pk) throws RcPsicoNivelSatisfaccionDaoException
	{
		long t1 = System.currentTimeMillis();
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			stmt = conn.prepareStatement( SQL_DELETE );
			stmt.setLong( 1, pk.getIdNivelSatisfaccion() );
			int rows = stmt.executeUpdate();
			long t2 = System.currentTimeMillis();
		}
		catch (Exception _e) {
			throw new RcPsicoNivelSatisfaccionDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns the rows from the RC_PSICO_NIVEL_SATISFACCION table that matches the specified primary-key value.
	 */
	public RcPsicoNivelSatisfaccion findByPrimaryKey(RcPsicoNivelSatisfaccionPk pk) throws RcPsicoNivelSatisfaccionDaoException
	{
		return findByPrimaryKey( pk.getIdNivelSatisfaccion() );
	}

	/** 
	 * Returns all rows from the RC_PSICO_NIVEL_SATISFACCION table that match the criteria 'ID_NIVEL_SATISFACCION = :idNivelSatisfaccion'.
	 */
	public RcPsicoNivelSatisfaccion findByPrimaryKey(long idNivelSatisfaccion) throws RcPsicoNivelSatisfaccionDaoException
	{
		RcPsicoNivelSatisfaccion ret[] = findByDynamicSelect( SQL_SELECT + " WHERE ID_NIVEL_SATISFACCION = ?", new Object[] {  new Long(idNivelSatisfaccion) } );
		return ret.length==0 ? null : ret[0];
	}

	/** 
	 * Returns all rows from the RC_PSICO_NIVEL_SATISFACCION table that match the criteria ''.
	 */
	public RcPsicoNivelSatisfaccion[] findAll() throws RcPsicoNivelSatisfaccionDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " ORDER BY ID_NIVEL_SATISFACCION", null );
	}

	/** 
	 * Returns all rows from the RC_PSICO_NIVEL_SATISFACCION table that match the criteria 'ID_NIVEL_SATISFACCION = :idNivelSatisfaccion'.
	 */
	public RcPsicoNivelSatisfaccion[] findWhereIdNivelSatisfaccionEquals(long idNivelSatisfaccion) throws RcPsicoNivelSatisfaccionDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE ID_NIVEL_SATISFACCION = ? ORDER BY ID_NIVEL_SATISFACCION", new Object[] {  new Long(idNivelSatisfaccion) } );
	}

	/** 
	 * Returns all rows from the RC_PSICO_NIVEL_SATISFACCION table that match the criteria 'TIPO = :tipo'.
	 */
	public RcPsicoNivelSatisfaccion[] findWhereTipoEquals(String tipo) throws RcPsicoNivelSatisfaccionDaoException
	{
		return findByDynamicSelect( SQL_SELECT + " WHERE TIPO = ? ORDER BY TIPO", new Object[] { tipo } );
	}

	/**
	 * Method 'RcPsicoNivelSatisfaccionDaoImpl'
	 * 
	 */
	public RcPsicoNivelSatisfaccionDaoImpl()
	{
	}

	/**
	 * Method 'RcPsicoNivelSatisfaccionDaoImpl'
	 * 
	 * @param userConn
	 */
	public RcPsicoNivelSatisfaccionDaoImpl(final java.sql.Connection userConn)
	{
		this.userConn = userConn;
	}

	/** 
	 * Sets the value of maxRows
	 */
	public void setMaxRows(int maxRows)
	{
		this.maxRows = maxRows;
	}

	/** 
	 * Gets the value of maxRows
	 */
	public int getMaxRows()
	{
		return maxRows;
	}

	/**
	 * Method 'getTableName'
	 * 
	 * @return String
	 */
	public String getTableName()
	{
		return "IDEA.RC_PSICO_NIVEL_SATISFACCION";
	}

	/** 
	 * Fetches a single row from the result set
	 */
	protected RcPsicoNivelSatisfaccion fetchSingleResult(ResultSet rs) throws SQLException
	{
		if (rs.next()) {
			RcPsicoNivelSatisfaccion dto = new RcPsicoNivelSatisfaccion();
			populateDto( dto, rs);
			return dto;
		} else {
			return null;
		}
		
	}

	/** 
	 * Fetches multiple rows from the result set
	 */
	protected RcPsicoNivelSatisfaccion[] fetchMultiResults(ResultSet rs) throws SQLException
	{
		Collection resultList = new ArrayList();
		while (rs.next()) {
			RcPsicoNivelSatisfaccion dto = new RcPsicoNivelSatisfaccion();
			populateDto( dto, rs);
			resultList.add( dto );
		}
		
		RcPsicoNivelSatisfaccion ret[] = new RcPsicoNivelSatisfaccion[ resultList.size() ];
		resultList.toArray( ret );
		return ret;
	}

	/** 
	 * Populates a DTO with data from a ResultSet
	 */
	protected void populateDto(RcPsicoNivelSatisfaccion dto, ResultSet rs) throws SQLException
	{
		dto.setIdNivelSatisfaccion( rs.getLong( COLUMN_ID_NIVEL_SATISFACCION ) );
		dto.setTipo( rs.getString( COLUMN_TIPO ) );
		reset(dto);
	}

	/** 
	 * Resets the modified attributes in the DTO
	 */
	protected void reset(RcPsicoNivelSatisfaccion dto)
	{
		dto.setIdNivelSatisfaccionModified( false );
		dto.setTipoModified( false );
	}

	/** 
	 * Returns all rows from the RC_PSICO_NIVEL_SATISFACCION table that match the specified arbitrary SQL statement
	 */
	public RcPsicoNivelSatisfaccion[] findByDynamicSelect(String sql, Object[] sqlParams) throws RcPsicoNivelSatisfaccionDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = sql;
		
		
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			throw new RcPsicoNivelSatisfaccionDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
		
	}

	/** 
	 * Returns all rows from the RC_PSICO_NIVEL_SATISFACCION table that match the specified arbitrary SQL statement
	 */
	public RcPsicoNivelSatisfaccion[] findByDynamicWhere(String sql, Object[] sqlParams) throws RcPsicoNivelSatisfaccionDaoException
	{
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT + " WHERE " + sql;
		
		
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			stmt.setMaxRows( maxRows );
		
			// bind parameters
			for (int i=0; sqlParams!=null && i<sqlParams.length; i++ ) {
				stmt.setObject( i+1, sqlParams[i] );
			}
		
		
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchMultiResults(rs);
		}
		catch (Exception _e) {
			throw new RcPsicoNivelSatisfaccionDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		}
	}
	
	/**
	 * Method 'getTableNameInnerJoin'
	 * 
	 * @return String
	 */
	public String getTableNameInnerJoin()
	{
		return "IDEA.RC_PSICOLOGIA";
	}
	
	/**
	 * Metodo que obtiene el objeto RcRehaNivelEducativo a traves del idRehabilitador
	 * 
	 * @param sql
	 * @param sqlParams
	 * @return
	 * @throws RcRehaProbSensorialesDaoException
	 */
	public RcPsicoNivelSatisfaccion findByInnerJoinRCRehabilitador( long idPsicologia ) throws RcPsicoNivelSatisfaccionDaoException{
		
		// declare variables
		final boolean isConnSupplied = (userConn != null);
		Connection conn = null;
		PreparedStatement stmt = null;
		ResultSet rs = null;
		
		try {
			// get the user-specified connection or get a connection from the ResourceManager
			conn = isConnSupplied ? userConn : ResourceManager.getConnection();
		
			// construct the SQL statement
			final String SQL = SQL_SELECT_INNER_JOIN 
					           + " INNER JOIN " + getTableNameInnerJoin()
					           + " ON " + getTableNameInnerJoin() + ".ID_NIVEL_SATISFACCION = " + getTableName() + ".ID_NIVEL_SATISFACCION"
					           + " WHERE " + getTableNameInnerJoin() + ".ID_RC_PSICOLOGIA =" + idPsicologia;
		
		
			// prepare statement
			stmt = conn.prepareStatement( SQL );
			rs = stmt.executeQuery();
		
			// fetch the results
			return fetchSingleResult(rs);
		}
		catch (Exception _e) {
			throw new RcPsicoNivelSatisfaccionDaoException( "Exception: " + _e.getMessage(), _e );
		}
		finally {
			ResourceManager.close(rs);
			ResourceManager.close(stmt);
			if (!isConnSupplied) {
				ResourceManager.close(conn);
			}
		
		}
	}

}
